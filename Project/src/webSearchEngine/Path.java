package webSearchEngine;

//Sets the path to store the web files and txt files. Please change the "/" to "\\" if running in Windows Operating system.

public class Path {
	public static String htmlDirectoryPath = System.getProperty("user.dir") + "/webFiles/";
	public static String txtDirectoryPath = System.getProperty("user.dir") + "/textfiles/";
}
